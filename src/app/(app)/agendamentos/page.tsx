'use client';

import { Agenda } from '@/components/agendamentos/Agenda';

/** Cargas a Enviar — agenda do montador de cargas. */
export default function AgendamentosPage() {
  return <Agenda tipo="enviar" />;
}
