'use client';

import { Agenda } from '@/components/agendamentos/Agenda';

/** Cargas a Receber — agenda do depósito. */
export default function RecebimentosPage() {
  return <Agenda tipo="receber" />;
}
